const Nav = () => {
    return (
        <nav className="flex items-center justify-between w-full">
            <button>연락처</button>
            <button>주소</button>
            <button>톡톡문의</button>
            <button>인스타그램</button>
        </nav>
    );
};

export default Nav;
